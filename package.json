{
  "name": "@brillout/depdev",
  "version": "0.0.3",
  "scripts": {
    "dev": "tsc --watch",
    "build": "rm -rf dist/ && tsc",
    "prepublishOnly": "npm run build",
    "release": "npm version patch && npm publish && git push && git push --tags"
  },
  "devDependencies": {
    "@types/node": "^15.12.2",
    "typescript": "^4.3.5"
  },
  "bin": {
    "depdev": "./bin.js"
  },
  "files": [
    "dist/"
  ],
  "license": "MIT",
  "repository": "github:brillout/depdev",
  "publishConfig": {
    "access": "public"
  }
}
